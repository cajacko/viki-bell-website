import { DEFAULT, INVERSE } from 'constants/sectionColours';

export default {
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },

  default: {
    background: DEFAULT,
  },

  inverse: {
    background: INVERSE,
  },
};
